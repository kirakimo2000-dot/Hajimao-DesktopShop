# v0.1.0 Gameplay Foundation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebase Hajimao at 0.1.0 and deliver a tested gameplay-first foundation for fixed-time operation, player progression, product/store unlocks, multiple stores, differentiated product economics, employee efficiency/wages, and tray-only Windows presence.

**Architecture:** Domain owns player, level curve, shared business wallet, store portfolio, products and employees. Application exposes a business service and immutable snapshots; the current single-store WPF UI remains an adapter and is not expanded. The existing prototype save is migrated compatibly while player speed is removed across all active contracts.

**Tech Stack:** .NET 10, C# 14, WPF, CommunityToolkit.Mvvm, SQLite, System.Text.Json, xUnit.

---

### Task 1: Rebase version history and current roadmap

**Files:**
- Modify: `Directory.Build.props`
- Create: `docs/archive/prototype-1.0.0-history.md`
- Modify: `CHANGELOG.md`
- Modify: `docs/roadmap.md`
- Modify: `task_plan.md`

- [ ] Archive the existing prototype release history without deleting artifacts.
- [ ] Set `VersionPrefix` to `0.1.0` and rewrite the active roadmap from 0.1.0 upward.
- [ ] Mark the former fixed-time 1.1 plan as superseded by this plan.

### Task 2: Remove player speed and migrate prototype saves

**Files:**
- Delete: `src/HajimaoDesktopShop.Application/Simulation/SimulationSpeed.cs`
- Modify: `SimulationClock.cs`, `ShopSimulation.cs`, `SimulationSnapshot.cs`, `GameSaveData.cs`
- Create: `src/HajimaoDesktopShop.Infrastructure/Persistence/LegacyGameSaveV1.cs`
- Modify: `SqliteGameSaveStore.cs`, `GameViewModel.cs`, `ManagementWindow.xaml` and affected tests

- [ ] RED: assert public simulation/save/UI contracts expose no speed control.
- [ ] GREEN: make one real second always process one simulation tick.
- [ ] RED: load schema-v1 payloads containing 0/1/2/4 speed values.
- [ ] GREEN: migrate all to schema v2 while discarding speed and preserving every other field.

### Task 3: Add player progression and level curve

**Files:**
- Create: `src/HajimaoDesktopShop.Domain/Players/PlayerProfile.cs`
- Create: `src/HajimaoDesktopShop.Domain/Players/LevelCurve.cs`
- Create: corresponding Domain tests

- [ ] RED/GREEN: start at level 1, gain non-negative XP, cross multiple thresholds, reject invalid curves and overflow.
- [ ] Keep total XP as the stored truth; derive level through the immutable curve.

### Task 4: Add shared-wallet multi-store portfolio

**Files:**
- Create: `BusinessWallet.cs`, `ShopId.cs`, `ShopDefinition.cs`, `RetailBusiness.cs`, `OpenShopResult.cs`
- Modify: `Shop.cs` and its tests
- Create: `RetailBusinessTests.cs`

- [ ] RED/GREEN: a new business starts with exactly one store.
- [ ] RED/GREEN: level gates store definitions; duplicates and insufficient funds are atomic failures.
- [ ] RED/GREEN: all stores share one business wallet, while revenue/cost totals remain per store.

### Task 5: Add product unlock metadata and economic metrics

**Files:**
- Modify: `Product.cs`, `ProductDefinition.cs`, `ProductSnapshot.cs`, JSON catalog and tests

- [ ] RED/GREEN: expose unit gross profit and gross-margin basis points without floating-point money.
- [ ] RED/GREEN: validate `requiredPlayerLevel >= 1`; preserve level 1 for absent legacy JSON values.
- [ ] Assign the ten products distinct unlock levels and retain deliberately different margins.

### Task 6: Add employee efficiency and hourly wage model

**Files:**
- Create: Domain `Employees/EmployeeId.cs`, `EmployeeRole.cs`, `Employee.cs`
- Move Application employee role consumers to the Domain enum
- Create: `EmployeeTests.cs`

- [ ] RED/GREEN: validate name, efficiency permille and hourly wage.
- [ ] RED/GREEN: calculate task minutes with integer ceiling; higher efficiency finishes sooner.
- [ ] RED/GREEN: accrue exact hourly wages over worked minutes using a remainder so fractional cents are never lost.

### Task 7: Add gameplay-first business application service

**Files:**
- Create: `Application/Business/BusinessGameService.cs`, commands/results/snapshots
- Create: `BusinessGameServiceTests.cs`

- [ ] RED/GREEN: initialize one starter store and only level-eligible products.
- [ ] RED/GREEN: successful sales award XP, level-ups unlock products, and product registration is idempotent.
- [ ] RED/GREEN: opening an eligible store charges the shared wallet and registers all currently unlocked products.
- [ ] Keep the existing `ShopGameService` as the current UI adapter until UI work begins.

### Task 8: Add tray-only Windows shell behavior

**Files:**
- Modify: Desktop csproj, `App.xaml`, `App.xaml.cs`, both window XAML files
- Create: `Desktop/Services/TrayIconService.cs`

- [ ] Set desktop and management windows `ShowInTaskbar=False`.
- [ ] Create one notification-area icon with open shop, open management and explicit exit actions.
- [ ] Keep simulation/autosave running while windows are hidden; dispose the tray icon on exit.

### Task 9: Verify and document 0.1.0

**Files:**
- Create: `docs/progress/v0.1.0-gameplay-foundation.md`
- Modify: `README.md`, `CHANGELOG.md`, architecture and planning files

- [ ] Run all Release tests/build and NuGet vulnerability audit.
- [ ] Verify speed references exist only inside legacy migration/history.
- [ ] Verify one-store start, level/product/store unlocks, shared wallet, product margins and employee wage math through automated tests.
- [ ] Run the packaged app and verify notification-area icon, no taskbar window button, management opening and explicit exit.
- [ ] Clean generated scratch files and record the next 0.2.0 gameplay plan.

